# Use supported Java image
FROM eclipse-temurin:17-jdk

# Set working directory
WORKDIR /app

# Copy JAR from target folder
COPY target/*.jar .

# Expose port
EXPOSE 8080

# Run the application
ENTRYPOINT ["sh","-c","java -jar $(ls *.jar)"]
